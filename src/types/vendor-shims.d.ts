/**
 * Ambient modules only when a package has no types and TS cannot resolve it.
 * Prefer real dependencies in package.json over stubs here.
 */
declare module "react-day-picker";
declare module "cmdk";
declare module "vaul";
declare module "react-resizable-panels";
